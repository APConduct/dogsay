[package]
name = "dogsay"
version = "0.1.0"
edition = "2024"
authors = ["APConduct"]
description = "A simple command-line tool for generating dog-themed ASCII art."

[dependencies]
clap = { version = "4.4", features = ["derive"] }
textwrap = "0.16"
